# FastAPI NLP/dog matching service
# This app currently has no required environment variables.
# Run it on port 8000 so the NestJS API can call:
#   http://localhost:8000/sentiment

# Optional convention for your run command or deployment platform
PORT=8000
